@using front.Models.ContentModels
@model IEnumerable<ProjectModel>

@{
    ViewData["Title"] = "Project";
}

<h2>Проекты</h2>

<p>
    <a asp-action="AddProject" class="btn btn-success">Добавить новый проект</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Заголовок</th>
            <th>Описание</th>
            <th>Картинка</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.Description</td>
                <td>@item.ImageUrl</td>
                <td>
                    <a asp-controller="Admin" asp-action="EditProject" asp-route-id="@item.Id" class="btn btn-primary">Редактировать</a>
                    <form method="post" asp-controller="Admin" asp-action="DeleteProject" asp-route-id="@item.Id" style="display:inline;">
                        <button type="submit" class="btn btn-danger">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
