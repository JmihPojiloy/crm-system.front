@using front.Models.ContentModels
@model IEnumerable<BlogPostModel>

@{
    ViewData["Title"] = "BlogPost";
}

<h2>Блог</h2>

<p>
    <a asp-action="AddBlogPost" class="btn btn-success">Добавить новый пост</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Заголовок</th>
            <th>Статья</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.Content</td>
                <td>
                    <a asp-controller="Admin" asp-action="EditBlogPost" asp-route-id="@item.Id" class="btn btn-primary">Редактировать</a>
                    <form method="post" asp-controller="Admin" asp-action="DeleteBlogPost" asp-route-id="@item.Id" style="display:inline;">
                        <button type="submit" class="btn btn-danger">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
